import { FilterCandidatePipe } from './filter-candidate.pipe';

describe('FilterCandidatePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterCandidatePipe();
    expect(pipe).toBeTruthy();
  });
});
