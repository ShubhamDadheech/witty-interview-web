import { FilterInterviewPipe } from './filter-interview.pipe';

describe('FilterInterviewPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterInterviewPipe();
    expect(pipe).toBeTruthy();
  });
});
